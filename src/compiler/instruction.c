#include "instruction.h"

uint8_t* doLine(const char*) {
    //translate line here
}

const char* removeNewLines(const char**) {
    //remove lines so it could go from seperator to seperator in increments
}

char translate(uint8_t* ins, const char* fname) {
    //entry for translation
}